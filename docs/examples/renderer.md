# renderer

**Target:** `example_renderer`  
**Feature:** Renderer world vs screen space

World sprite is drawn through ResourceManager (camera transform). A second copy is drawn in screen space via `Renderer::BufferAdd` without a camera.

## Assets

- `assets/sprites/crew_1.png`

## Controls

- **Escape** — quit
