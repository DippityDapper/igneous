# database

**Target:** `example_database`  
**Feature:** Database SQLite wrapper

Click counter is stored in a local SQLite database and restored on launch.

## Assets

- `assets/scores.db` — created on first run

## Controls

- **Increment and save** button — bump counter and write SQL
- **Escape** — quit
