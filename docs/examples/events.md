# events

**Target:** `example_events`  
**Feature:** Event publish–subscribe

Connects a callback to `Engine::Event<void, int>` and emits values from a button press.

## Assets

None.

## Controls

- **Emit ping** — fire event with incrementing payload
- **Escape** — quit
