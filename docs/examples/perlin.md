# perlin

**Target:** `example_perlin`  
**Feature:** Perlin noise generation

Generates a noise image, saves a PNG under assets, and displays it in screen space. Sliders adjust octaves and scale.

## Assets

- `assets/noise_preview.png` — written at runtime

## Controls

- **Regenerate** — rebuild noise with current settings
- **Escape** — quit
