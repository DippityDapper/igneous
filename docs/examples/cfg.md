# cfg

**Target:** `example_cfg`  
**Feature:** CFGParser load/save

Sliders write volume and fullscreen flags to a key-value config file at runtime.

## Assets

- `assets/settings.cfg` — created/updated on first run

## Controls

- Adjust **ImGui** controls to persist values
- **Escape** — quit
