# identity

**Target:** `example_identity`  
**Feature:** LocalIdentity, IdentityProvider

Reads local user id and username from a text file and can generate a simple auth token.

## Assets

- `assets/user.txt` — contains `id=` and `username=` lines

## Controls

- **Generate auth token** — show token byte size
- **Escape** — quit
